# 맥용 단축키 중심 클립보드 앱 스펙 (초안)

## 목적
- `CopyClip - Clipboard History`(App Store)와 Windows의 Win+C/V 경험을 참고하여, macOS에서 마우스 사용 없이 전역 단축키로 모든 클립보드 흐름을 제어하는 앱의 방향성을 정리한다.

## 제품 비전
- 전역 단축키 한 번으로 히스토리 패널을 열고, 키보드만으로 항목 탐색/붙여넣기를 즉시 수행한다.
- 무마우스 사용성, 경량 백그라운드 동작, 최소한의 UI 요소에 집중한다.
- CopyClip의 핵심 기능(히스토리, 검색, 저장 제한, 삭제)을 그대로 수용하되, 추가적인 접근성 흐름을 제공한다.

## 사용자 흐름
1. 사용자가 문서/브라우저 등에서 텍스트를 복사한다.
2. 전역 단축키(예: `⌃⌥V`)를 누르면 히스토리 패널이 즉시 레이어로 나타난다.
3. 방향키/숫자키/검색으로 원하는 항목을 선택하고 `Enter`를 누르면 현재 포커스 앱에 붙여넣는다.
4. 패널은 자동 닫히고, 사용자는 작업을 재개한다. 필요한 경우 `Esc`로 수동 종료 가능.
5. 사용자가 `⌘⇧5`로 스크린샷을 생성하면(Desktop 저장 기준), 앱이 신규 스크린샷 파일을 감지해 이미지 클립으로 히스토리에 추가하고 시스템 클립보드에도 자동 반영한다.

## UX/기능 원칙
- 즉시성: 열기 지연 150ms 이내, 단축키 누름 즉시 표시
- 무마우스: 모든 동작에 키보드 경로 제공
- 단순 메뉴: 기본 화면은 히스토리 목록 + 검색바 + 상태 표시
- 예측 가능성: 선택 후 `Enter`는 항상 붙여넣기, `⌘⌫`는 삭제

## MVP 기능
- 클립보드 히스토리 저장
  - 시스템 클립보드 변경 감지 (텍스트 + 이미지)
  - 중복 항목은 순위 재조정
  - 스크린샷(`⌘⇧5`) 파일 자동 감지 후 클립보드/히스토리 자동 반영(설정 토글 가능)
- 전역 단축키 패널
  - `⌃⌥V`(초기 추천, 사용자 지정 가능)
  - 패널은 화면 중앙 또는 마지막 사용 위치
- 키보드 탐색/붙여넣기
  - `↑/↓`, `⌘1..9` 키 입력, 검색 타이핑 후 `Enter`
  - `Esc` 닫기
- 검색 필터
  - 패널 오픈 직후 타이핑 시 실시간 필터
- 설정/관리
  - 시작 시 자동 실행
  - 최대 저장 개수 설정(50/100/200)
  - 개별 삭제(`⌘⌫`), 전체 삭제(`⇧⌘⌫`)
  - 클립 기록 일시 중지 토글
  - 스크린샷 자동 클립보드 저장 토글(기본값: ON)
- 메뉴바 상주
  - 메뉴바 아이콘으로 상태 확인 + 단축키 안내

## CopyClip 참고 기능 매핑
- 히스토리 저장 및 목록 표시: MVP 동일 구현
- 검색 바 필터: MVP 포함
- 개별/전체 삭제: MVP 포함
- 저장 개수(remember count) 설정: MVP 포함
- 자동 시작: MVP 포함
- 앱 예외 목록, 핀 항목, 클립 편집, 붙여넣기 옵션: Beta 이후
- 일반 파일 클립, 클라우드 동기화: 초기 범위 제외
- 이미지 클립은 MVP 확장 범위로 포함(클립보드 이미지 + 스크린샷 자동 반영)

## 단축키 초안
- 패널 열기: `⌃⌥V`
- 선택 항목 붙여넣기: `Enter`
- 단축키 탐색: `↑/↓`, `⌘1..9`
- 검색 시작: 오픈 상태에서 바로 타이핑
- 삭제: `⌘⌫` (선택 항목), `⇧⌘⌫` (전체)
- 패널 닫기: `Esc`
- 복사 기본 동작: `⌘C`는 시스템 복사 유지

## 요구사항
- 전역 단축키 충돌 감지 및 재정의 옵션 제공
- 붙여넣기는 현재 포커스 앱에 수행
- 로컬 저장 기본, 민감 앱 제외 옵션은 Beta 반영
- 메뉴바 앱으로 경량 백그라운드 동작
- 접근성 권한 안내와 수동 붙여넣기 대체 흐름 제공
- Desktop 경로의 신규 스크린샷 파일 감지 시, 이미지를 시스템 클립보드와 앱 히스토리에 자동 저장

## 리스크/대응
- 접근성 권한 없이 붙여넣기 실패: 온보딩 권한 안내 + 수동 복사 안내
- 단축키 충돌: 기본값 충돌 감지 후 추천 키 제안
- 클립보드 누락: 이벤트 버퍼링/예외 로그

## 출시 단계
1. Alpha: 텍스트 히스토리 + 단축키 패널 + `Enter` 붙여넣기
2. Beta: 검색 고도화, 설정(개수 제한/삭제), 자동 실행, 접근성 안내
3. v1.0: 앱 예외 목록, 핀, 클립 편집, 사용자 단축키 저장

## 배포/설치 기준
- 로컬 배포 산출물은 `ClipboardApp.app` 및 더블클릭 설치용 `ClipboardApp-installer.pkg`를 기본 제공한다.
- 실사용 배포는 Developer ID 기반 코드 서명 + notarization 완료본을 기준으로 한다.
- 서명/노터라이즈 자동화는 `scripts/sign_and_notarize.sh`, `fastlane/Fastfile`, `.github/workflows/release.yml` 기준으로 관리한다.

## 문서 체계
- 제품 스펙(SSOT): `SPEC.md`
- 에이전트 작업 규칙: `AGENTS.md`
- 브랜치/커밋/병합 규칙: `CONTRIBUTING.md`
- 보안/비밀정보 규칙: `SECURITY.md`
- 변경 이력: `docs/CHANGE_LOG.md`

## 참조
- CopyClip - Clipboard History (App Store 참조)
- FIPLAB Support 문서 (CopyClip FAQ 및 문제 해결)
